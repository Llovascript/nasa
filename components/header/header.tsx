import React from "react";
import { View, text } from 'react-native';

const Header = () => {
    return (
        <View>
            <Text>Header</Text>
        <View/>
    )
};

export default Header;